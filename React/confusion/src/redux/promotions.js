import { PROMOTIONS } from '../shared/promotions';

export const Promotions = (state = PROMOTIONS, action) => { // eslint-disable-line
  switch (action.type) {
    default:
      return state;
  }
};
